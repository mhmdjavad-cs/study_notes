
مجموعه‌ی $\Gamma$ در زبان L مدل دارد <-> هر زیر مجموعه‌ی متناهی از آن، مدل داشته باشد.


اثبات:
یه ورش که سادس.
برای سمت دیگه فرض کن $\Gamma$ مدل نداشته باشه، پس $\Gamma$ ناسازگاره. در نتیجه گاما میده $\bot$ رو. در نتیجه یه زیر مجموعه ی متنهای ازش هم میشه پیدا کرد که بده بات رو.

